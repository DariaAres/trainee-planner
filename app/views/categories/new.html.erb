<div class="row align-items-center border-form">
  <div class="border">
    <h1><%= t('category.new_c') %></h1>

    <%= render partial: 'categories/form', locals: { category: @category, button: 'create' } %>
  </div>
</div>
